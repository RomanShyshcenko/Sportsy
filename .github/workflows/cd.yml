name: Upload to EC2

on:

  workflow_run:
      workflows: [ "CI Pipeline" ]
      types:
        - completed

concurrency:
  group: main
  cancel-in-progress: true


jobs:
  deploy:
    name: Deploy
    runs-on: self-hosted
    steps:
      - name: Run deploy
        run: |
            cd ..
            sudo docker-compose up --build